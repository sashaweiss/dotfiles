#!/usr/bin/env zsh

alias cd='cd -P'
alias "cd.."='cd ..'

alias d='date'

alias dot="cd $DOTFILES"

alias e="$EDITOR"

alias untar='tar -xzvf'

alias desk="cd $DESK"
alias down="cd $DOWN"
alias work="cd $WORK"

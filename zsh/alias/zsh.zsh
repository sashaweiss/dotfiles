#!/usr/bin/env zsh

alias cd='cd -P'
alias "cd.."='cd ..'

alias d='date'

alias dot="cd $DOTFILES"

alias pg='ping google.com'

alias e="$EDITOR"

alias l='ls -lFh'
alias la='ls -alhF'

alias untar='tar -xzvf'

alias work="cd $WORK"
alias notes="cd $NOTES"
alias play="cd $PLAY"
alias down="cd $DOWN"


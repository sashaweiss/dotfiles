# TODO: varargs for multiple columns

awk_command=""
delim=""
if [ "$#" -eq 1 ]; then
  awk_command="{ print \$$1 }"
elif [ "$#" -eq 2 ]; then
  awk_command="{ print \$$2 }"
  delim="-F $1"
else
  echo "Wrong number of arguments!"
  exit 1
fi

cmd="awk $delim '$awk_command'"
eval $cmd <&0
